import { NavLink, Outlet, useSearchParams } from "react-router-dom"



//Список товаров, фильтрация
//products

export default function ProductsPage({ token, setToken }) {
    return (
        <div>
            <h2>Products Page</h2>
            <p>List of products will be displayed here.</p>
        </div>
    )
}